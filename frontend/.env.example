# Base URL for the Indian Stock Market API (no trailing slash).
# With Vite dev server proxy, use empty or /api so requests go to backend.
# For a separate backend, e.g. http://localhost:8000
VITE_API_URL=/api
